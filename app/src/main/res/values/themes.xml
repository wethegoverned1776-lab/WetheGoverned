<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Minimal theme to avoid system splash interference -->
    <style name="Theme.WeTheGoverned.Splash" parent="android:Theme.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <!-- Main App Theme -->
    <style name="Theme.WeTheGoverned" parent="android:Theme.NoTitleBar">
        <item name="android:windowBackground">@color/white</item>
    </style>

</resources>